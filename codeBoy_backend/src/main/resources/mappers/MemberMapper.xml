<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeboy.mvc.model.dao.MemberDao">
    <select id="selectMemberById" parameterType="long" resultType="Member">
        SELECT *
        FROM member
        WHERE member_id = #{memberId}

    </select>
    <update id="deactivateMemberById" parameterType="long">
        UPDATE member
        SET is_active = 0
        WHERE member_id = #{memberId}

    </update>
    <update id="updateMemberById">
        UPDATE member
        <set>
            <if test="update.nickname != null">
         nickname = #{update.nickname}
            </if>
            <if test="update.id != null">
                id = #{update.id}
            </if>
            <if test="update.email != null">
                email = #{update.email}
            </if>
        </set>
        WHERE member_id = #{memberId}
    </update>

    <select id="existsId" parameterType="String" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM member
        WHERE id = #{id}
    </select>

    <select id="existsNickname" parameterType="String" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM member
        WHERE nickname = #{nickname}
    </select>

    <select id="existsEmail" parameterType="String" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM member
        WHERE email = #{email}
    </select>

    <select id="isMemberActive" parameterType="long" resultType="boolean">
        SELECT is_active
        FROM member
        WHERE member_id = #{memberId}
    </select>
</mapper>
