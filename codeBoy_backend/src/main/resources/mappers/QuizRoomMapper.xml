<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mappper namespace="com.codeboy.mvc.model.dao.QuizRoomDao">

	<!-- 채팅방 생성 -->
	<insert id="insertQuizRoom" useGeneratedKeys="true"
		keyProperty="roomId">
		INSERT INTO
		quiz_room()
		VALUES();
	</insert>


	<!-- 채팅방 참가 -->
	<insert id="insertMemberToQuizRoom"
		parameterType="QuizRoomMember">
		INSERT INTO
		quiz_room_member(member_id, room_id, is_host)
		VALUES(#{memberId, roomId, isHost});
	</insert>


	<!-- 모든 퀴즈룸 조회 -->
	<select id="selectAllQuizRoom" resultType="QuizRoom">
		SELECT *
		FROM
		quiz_room;
	</select>

	<!-- 하나의 퀴즈룸 조회 -->
	<select id="selectOneQuizRoom" resultType="QuizRoomMember"
		parameterType="long">
		SELECT *
		FROM quiz_room_member
		WHERE room_id = #{roomId}
	</select>

	<!-- 퀴즈룸 삭제 -->
	<delete id="deleteQuizRoom" parameterType="long">
		DELETE FROM quiz_room_member
		WHERE room_id = #{roomId}

		DELETE FROM quiz_room
		WHERE room_id = #{roomId};
	</delete>


</mappper>